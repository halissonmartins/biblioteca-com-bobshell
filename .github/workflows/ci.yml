name: CI

on:
  push:
    branches: ["**"]
  pull_request:
    branches: [main, "feat/**"]

jobs:
  # ---------------------------------------------------------------------------
  # API — typecheck · lint · testes (Vitest)
  # ---------------------------------------------------------------------------
  api-ci:
    name: "API — typecheck · lint · test"
    runs-on: ubuntu-latest

    services:
      postgres:
        image: postgres:15
        env:
          POSTGRES_USER: biblioteca
          POSTGRES_PASSWORD: biblioteca
          POSTGRES_DB: biblioteca_test
        ports:
          - 5432:5432
        options: >-
          --health-cmd "pg_isready -U biblioteca"
          --health-interval 10s
          --health-timeout 5s
          --health-retries 5

    env:
      DATABASE_URL: postgresql://biblioteca:biblioteca@localhost:5432/biblioteca_test
      JWT_SECRET: ci-jwt-secret-placeholder
      JWT_REFRESH_SECRET: ci-jwt-refresh-secret-placeholder
      NODE_ENV: test

    defaults:
      run:
        working-directory: packages/api

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node.js 20
        uses: actions/setup-node@v4
        with:
          node-version: "20"
          cache: "npm"
          cache-dependency-path: packages/api/package-lock.json

      - name: Instalar dependências
        run: npm ci

      - name: Gerar Prisma Client
        run: npx prisma generate

      - name: Aplicar migrations
        run: npx prisma migrate deploy

      - name: Typecheck
        run: npm run typecheck

      - name: Lint
        run: npm run lint

      - name: Testes (Vitest)
        run: npm run test

  # ---------------------------------------------------------------------------
  # Web — typecheck · lint (oxlint) · build (Vite)
  # ---------------------------------------------------------------------------
  web-ci:
    name: "Web — typecheck · lint · build"
    runs-on: ubuntu-latest

    defaults:
      run:
        working-directory: packages/web

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node.js 20
        uses: actions/setup-node@v4
        with:
          node-version: "20"
          cache: "npm"
          cache-dependency-path: packages/web/package-lock.json

      - name: Instalar dependências
        run: npm ci

      - name: Typecheck
        run: npx tsc -b

      - name: Lint (oxlint)
        run: npm run lint

      - name: Build de produção
        run: npm run build

  # ---------------------------------------------------------------------------
  # Gate: bloqueia merge se qualquer job falhou
  # ---------------------------------------------------------------------------
  ci-gate:
    name: "CI Gate"
    runs-on: ubuntu-latest
    needs: [api-ci, web-ci]
    if: always()
    steps:
      - name: Verificar resultado dos jobs
        run: |
          if [[ "${{ needs.api-ci.result }}" != "success" || "${{ needs.web-ci.result }}" != "success" ]]; then
            echo "❌ CI falhou — api-ci: ${{ needs.api-ci.result }}, web-ci: ${{ needs.web-ci.result }}"
            exit 1
          fi
          echo "✅ Todos os checks passaram."
